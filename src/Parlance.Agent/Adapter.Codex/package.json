{
  "name": "@parlance/agent-adapter-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "parlance-agent-codex": "dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json && node scripts/bundle.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@parlance/agent-core": "file:../Core",
    "@types/node": "^22.9.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  }
}
