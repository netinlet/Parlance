{
  "name": "@parlance/agent-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "parlance-agent-core": "dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json && node scripts/bundle.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  }
}
